<odoo>
    <!-- Vista de Lista para 'library.author' -->
    <record id="view_library_author_tree" model="ir.ui.view">
    <field name="name">library.author.list</field>
    <field name="model">library.author</field>
    <field name="arch" type="xml">
        <list string="Authors">
            <field name="name"/>
            <field name="birthdate"/>
        </list>
    </field>
</record>

    <!-- Vista de formulario para 'library.author' -->
    <record id="view_library_author_form" model="ir.ui.view">
        <field name="name">library.author.form</field>
        <field name="model">library.author</field>
        <field name="arch" type="xml">
            <form string="Author">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="birthdate"/>
                        <field name="biography"/>
                    </group>
                    <notebook>
                        <page string="Books">
                            <field name="book_ids">
                                <list>
                                    <field name="name"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Acción para abrir la vista de autores -->
    <record id="action_library_author" model="ir.actions.act_window">
        <field name="name">Authors</field>
        <field name="res_model">library.author</field>
        <field name="view_mode">list,form</field>
        <field name="target">current</field>
    </record>

    <!-- Menú para acceder a los autores -->
    <menuitem id="menu_library_author" name="Authors" action="action_library_author" parent="base.menu_custom"/>
</odoo>
